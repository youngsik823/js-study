// 구구단 3단 출력

// var level = +prompt(`단을 입력하세요.`);


for (var i = 1; i <= 9; i++) {
    for (var j = 1; j <= 9; j++) {
        console.log(`${i} x ${j} = ${i*j} `);
    }
   console.log(`=======================`); 
}